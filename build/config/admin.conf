server {
    listen 18080;
    # server_name 61.153.190.250.nip.io;
    location / {
        root /usr/share/nginx/fe;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}

